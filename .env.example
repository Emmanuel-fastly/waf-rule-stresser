# Backend Configuration
# Port for the Go backend server (default: 8080)
PORT=8080

# Frontend Configuration (for development)
# Port for the Vite dev server (default: 3000)
VITE_PORT=3000

# Backend API URL that frontend connects to (default: http://localhost:8080)
VITE_API_URL=http://localhost:8080
